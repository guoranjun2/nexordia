# Task: Add editing tool confirmation and consent
- **Task Identifier:** 2026-01-12-consent
- **Scope:** Add per tool confirmation dialogs and consent handling for editing tools, with separate confirmations for Model Context Protocol mode and large language model mode. Provide separate preferences only for read, edit, create, and restructure tool groups, with each preference covering all tools in that group. Show the user summary in the user chat before the confirmation dialog when the dialog should appear, and surface tool unavailable errors in the message box and in the model response.
- **Motivation:** Editing operations need explicit user consent that is specific to each tool and to the interaction mode.
- **Research:** OptionalDontShowMeAgainDialog requests cannot include motivation strings because they are standard. Confirm how OptionalDontShowMeAgainDialog is used in Freeplane features and how it is represented in preferences.
- **Design:** Use OptionalDontShowMeAgainDialog per tool, not as a global setting. Store separate confirmation preferences for Model Context Protocol mode and large language model mode. Each confirmation preference covers all tools in its group (read, edit, create, restructure). Do not include motivation strings in OptionalDontShowMeAgainDialog requests. Show the user summary in the user chat before the confirmation dialog when the dialog should appear. If a tool is unavailable, return the error to the model so it can request a settings change. Limit confirmation options to read, edit, create, and restructure tools.
- **Test specification:** Verify each tool shows its own confirmation dialog and stores its own preference. Verify Model Context Protocol mode and large language model mode have separate confirmation preferences. Verify the summary appears in user chat before the confirmation dialog when the dialog appears. Verify a tool unavailable error is shown in the message box and returned to the model. Verify denied confirmation prevents the edit and returns an error.
