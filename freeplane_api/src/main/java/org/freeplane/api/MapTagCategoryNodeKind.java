package org.freeplane.api;

/**
 * Node kinds used in {@link MapTagCategoryNode}.
 * @since 1.13.3
 */
public enum MapTagCategoryNodeKind {
    /** Category node in the category tree. */
    CATEGORY,
    /** Tag node stored under a category node. */
    TAG
}
